<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.PhotoMapper">

    <!-- ① 写真追加 -->
    <insert id="insert" parameterType="Photo">
        INSERT INTO photos
        (restaurant_id, request_restaurant_id, user_id, url, status, created_at, updated_at)
        VALUES
        (#{restaurantId}, #{requestRestaurantId}, #{userId}, #{url}, #{status}, NOW(), NOW())
    </insert>

    <!-- ④ レストラン別写真一覧 -->
    <select id="findByRestaurantId" resultType="Photo">
        SELECT *
        FROM photos
        WHERE restaurant_id = #{restaurantId}
        ORDER BY created_at DESC
    </select>

    <!-- ⑤ ユーザー別写真一覧 -->
    <select id="findByUserId" resultType="Photo">
        SELECT *
        FROM photos
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- ⑥ ステータス別写真一覧 -->
    <select id="findByStatus" resultType="Photo">
        SELECT 
        p.*,
        r.name AS restaurantName,
        u.name AS userName
        FROM photos p
        LEFT JOIN restaurants r
        ON p.restaurant_id = r.restaurant_id
        LEFT JOIN users u
        ON p.user_id = u.user_id
        WHERE p.status = #{status}
        ORDER BY p.created_at DESC
    </select>

    <select id="findApprovedByRestaurantId" resultType="Photo">
        SELECT *
        FROM photos 
        WHERE restaurant_id = #{restaurantId}
          AND status = 'approved'
        ORDER BY created_at DESC
    </select>

    <update id="updateRestaurantIdByRequestId">
        UPDATE photos
        SET restaurant_id = #{restaurantId},
        updated_at = NOW()
        WHERE request_restaurant_id = #{requestRestaurantId}
    </update>

    <update id="update">
        UPDATE photos
        <set>
            status = #{status},
            <if test="rejectReason != null">
                reject_reason = #{rejectReason},
            </if>
            url = #{url},
            updated_at = NOW()
        </set>
        WHERE photo_id = #{photoId}
    </update>

    <select id="findRestaurantIdByPhotoId" resultType="Long">
        SELECT restaurant_id
        FROM photos
        WHERE photo_id = #{photoId}
    </select>
</mapper>
